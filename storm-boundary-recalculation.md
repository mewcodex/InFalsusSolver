# 风暴配卡：实际事件队列修正（2026-09-19）

已用当前 GameAssembly.dll 的实际调用关系修正风暴模型，并同时重现 Cryogenic、Ghost Ray 两份记录。原先 576388.38 分是错误的“一次性入队”模型结果，已撤下，不能与修正后结果比较优劣。

## 实际执行顺序

1. `_WMA` 在判定前调用 `_wmA`，允许进入和退出；判定完成、计数递增后再调用一次，此时只允许退出。之前将这两个布尔参数的含义读反了。
2. `_wmA` 先为双方生成阶段切换事件，再由 `_XmA` 按卡位、特质槽、Effect 顺序生成持续效果的加入／移除事件。`_XmA` 不生成瞬间特质。
3. `_xMA` 排序并执行队列。进入事件调用 `_WmA`，退出事件调用 `_xmA`，遍历同侧卡牌，追加满足触发条件的瞬间特质，然后重新排序尚未消费的队列。
4. 每次瞬间攻击读取当时的攻防和颜色封锁状态。阶段末计数已递增，当前颜色可能是下一张卡；整曲结束仍钳制为第五张卡。
5. 当前版本事件排序键保持默认零；原生 ArraySortHelper 对超过 16 个等键元素的排序不稳定。不能用 JavaScript 的稳定排序替代，也不能规定“增益恒先于攻击”或“按卡位一次性执行”。特质槽顺序仍应保持与展示方案一致。

原生地址：`_WMA` 0x4B1BA0、`_qMA` 0x4B18B0、`_wmA` 0x497700、`_XmA` 0x496FD0、`_WmA` 0x496C10、`_xmA` 0x497AF0、`_xMA` 0x4B3CE0、`_XMA` 0x4B27F0；比较器 0x496AF0，Introsort 0xAD5830，分区函数 0xAD70B0。对应 DLL 修改日期为 2026-09-18。

## 实测回归与输入边界

- Ghost Ray Forbidden：1484 判定，分配 297/297/296/297/297。仅用表现伤害上限 1312103 推回攻击缩放，独立预测特性伤害 1365171.07（实测 1365171）。敌方表现伤害 37098.98、特性伤害 54574.50 均吻合。以回复 78668.49 推回基础血量 393342.45，末阶段防御为 360419.51，显示 360420。合并实测表现伤害 1309273 后重现攻击率 77859、防御率 51488。
- 这套旧配卡前三次阶段结束共九次回响有效，第四阶段结束和整曲结束共六次回响被封锁。
- Cryogenic：五阶段各 196 判定；用用户报告的阶段 2–4 起始攻防 588133/439813 推回基础属性，沿用此前假设的 NEAR 分配 1/1/3/0/0，重现攻击率 84128.9487、防御率 45132.8567、分数 379697.9786，显示值全部吻合。此前少算的部分是阶段进入时的瞬间攻击：它们实际追加在持续效果事件之后。

这些是带有上述参考输入的回归，不是完整游戏事件日志验证；没有利用实测特性伤害反向调整技能倍率，也没有声称已验证所有特质组合。

运行 `node tools/storm-model-tests.mjs` 同时执行两组当前模型回归，并检查页面方案；历史汇总模型仅保留作对照，不再承担实测验证。

## 重新搜索和本地页面

使用 1382 种已知制造方案／颜色候选快照。修正后随机搜索 1059348 次评估，单卡替换、特质调整、槽位排列及前 24 候选双卡组合搜索 1015756 次评估。没有穷举联合空间，未证明全局最优。

同为 15 级、999 效能、准确率 100%、五阶段等物量参考点：旧 Ghost Ray 配卡修正后 437326.92，本轮找到 532659.39，提高约 21.80%。995 效能、11 级参考分 492283.24。准确率 100% 只是评分参考点，不是使用门槛。

本轮卡序为红命令、绿痛楚与憎恨、紫永远陪在你身旁、蓝不屈之力、红无双，特质已重新优化。具体槽序与完整拼法见本地 `team.html?loadout=storm-score`。

所有五张卡的制造数值、可用颜色、特质槽、不重复卡名、同卡特质不重复和最大三重叠限制均重新验证。本轮更新了本地页面，未部署远端。后台解库扩充继续运行；这里使用固定候选快照，未宣称包括后台随后产生的全部新记录。
