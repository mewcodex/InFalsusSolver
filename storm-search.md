# 风暴配卡：颜色克制修正（2026-09-19）

从当前已知解库构造 544 个拼法／颜色候选；50 种可掉落的非空特质全部参与搜索。
卡名不能重复，特质只在单卡内不可重复，成品攻防都必须大于零。拼法经过每格最多三粒子的原始几何检查。

双方先逐卡应用颜色克制（有利的卡攻击、防御乘 1.25），再汇总；满级联觉和谱面等级倍率作用于我方。旧方案第五位红色被蓝色克制，敌方总攻防均提高 5%，原来的忽略克制评分不能直接用于实际遭遇。

最终参考目标：粒子 999、15 级、五阶段等判定数，准确率 100% 作为比较参考点，并要求遭遇成功。最高已知分为 553707.0804519481；旧方案在同样正确模型下为 425913.0167971907，提高约 30.00%。

当前卡组：蓝色不屈之力、绿色痛楚与憎恨、紫色窥见深渊、红色无双、黄色永远陪在你身旁。完整拼法与技能随 `dist/team-score-data.json` 保存。

995 粒子、11 级的同一配卡参考分为 511737.98312099493。逐张重新计算 float32 效能倍率和向上取整后的成品属性，没有仅将最终分数按比例缩放。

## 搜索边界

- 多轮随机／退火搜索，另以旧方案和随机卡组作为起点。
- 对最终方案穷举单卡替换：同时替换拼法、颜色和该卡任意合法技能组合，共 10254498 次评估，没有提升。因此它在“其他四卡完全固定”的单卡替换邻域内最优。
- 检查 1860428 个双卡候选组合。每张替换卡使用相对于原卡组单独优化的技能；没有穷举两卡技能的笛卡尔积，不能称为完整双卡邻域证明。
- 从双卡候选及卡位排列得到 250 个起点，再做技能搜索，没有提升。
- 没有穷举所有五卡及技能组合，也没有全局上界证书。因此只能称为最高已知可行方案，不能称为全局最优，即使限定在当前已知拼法库中。

## 复现

在仓库目录运行：

```powershell
node tools/storm-model-tests.mjs
node tools/audit-known-solutions.mjs
node mechanism-tests.mjs
node tools/storm-search.mjs 19007 1 50000 dist/team-score-data.json storm-search-result.json
node tools/storm-polish.mjs storm-search-result.json storm-polished.json
```

搜索参数为随机种子、EXACT 概率（其余 BREAK）、时限毫秒、初始方案、输出路径。搜索脚本不直接修改网页数据。
`storm-polish` 输出五份 neighbors 文件，记录每个位置候选卡的最佳单卡技能配置。

`storm-model.mjs` 从网站当前解库构建候选。`prepare(state, level, potency)` 默认 15／999。`simulate` 默认均匀 EXACT/BREAK 模型；指定 phaseCounts 和 nears 则使用离散 EXACT/NEAR 模型（此路径无 BREAK）。

回归实测：Cryogenic ULT 980 判定；第 2–4 阶段面板 588133／439813。NEAR 分布 [1,1,3,0,0] 时，模型得到攻击率 84128.949、防御率 45132.857、总分 379697.979，均与玩家给出的四舍五入值吻合。该分布是匹配例，不是从总分唯一确定的失误顺序。
