import fs from 'node:fs';import assert from 'node:assert/strict';import {solve,key} from '../dist/solver.js';import {hasLegalOverlap} from '../dist/placement-rules.js';const data=JSON.parse(fs.readFileSync('dist/data.json')),cases=[[70,[1,2,4,7,9,10]],[72,[3,7,13,14,15]],[66,[0,2,3,4,6,7,8]],[67,[0,2,4,5,6]]],rows=[];for(const [id,areas]of cases){const recipe=data.recipes.find(r=>r.id===id);for(const strategy of ['legacy','portfolio','grasp']){let captured=0;const r=solve({recipe,iotas:data.iotas,skills:data.characters[recipe.character].skills,targets:[...new Set(areas.flatMap(i=>recipe.areas[i].cells.map(key)))],timeMs:500,seed:193019,searchStrategy:strategy,exploreAfterZero:true,onCandidate:r=>{assert.ok(hasLegalOverlap(r.placements));captured++;}});assert.ok(hasLegalOverlap(r.placements));rows.push({id,strategy,penalty:r.score.total,iterations:r.iterations,captured});}}fs.writeFileSync('local-data/cover-ensemble-benchmark.json',JSON.stringify(rows,null,2));console.log(rows);
